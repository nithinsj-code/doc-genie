[build]
  command = "echo \"window.DOCGENIUS_API_BASE = '${DOCGENIUS_API_BASE:-https://doc-genie.onrender.com}';\" > config.js"
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
